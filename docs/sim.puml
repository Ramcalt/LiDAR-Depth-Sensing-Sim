# https://plantuml.com/class-diagram

@startuml
interface Open3D{
}
hide Open3D members

abstract Object{
    -vec3<float> pos,
    -vec3<float> norm
    +setPos(vec3<float> pos)
    +getPos(vec3<float> pos)
    +setNorm(vec3<float> norm)
    +getNorm(vec3<float> norm)
}

class Surface extends Object{
}
class Emitter extends Object{
}
class Detector extends Object{
}

class SceneEdit{
    -tuple<Object, int> obj_list
    +int add(Object obj)
    +bool del(Object obj)
    +void setView(int obj_id)
    +void setView(float x, y, z, rx, ry, rz)
    +void moveView(float dx, dy, dz, drx, dry, drz)
}

class RayTracer{
    +PSD simulate()
}

class LaserSafety{
    +int safetyClass()
}

class SPAD{
}

class HistogramProcessing{
    +float getD
    epth()
}

SceneEdit --> Object
SceneEdit --> Open3D
SceneEdit --> RayTracer
LaserSafety --> RayTracer
SPAD --> RayTracer
HistogramProcessing --> SPAD


@enduml